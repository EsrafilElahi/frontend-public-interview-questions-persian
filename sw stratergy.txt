
Cache First: ابتدا کش را بررسی میکند و اگر داده موجود نباشد، از شبکه درخواست میدهد.

Network First: اول از شبکه درخواست میدهد و در صورت خطا، از کش استفاده میکند.

Stale-While-Revalidate: همزمان داده را از کش نمایش میدهد و در پسزمینه درخواست شبکه را برای آپدیت کش ارسال میکند.

